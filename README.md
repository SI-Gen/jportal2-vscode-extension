# SI Language Extension

This extension is a language server for the SI language and offers the following features:
* Syntax highlight
* Syntax checking
* Context aware autocomplete
* Hover information

